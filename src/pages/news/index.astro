---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import Nav from '../../components/Nav.astro';
import Footer from '../../components/Footer.astro';

const b = import.meta.env.BASE_URL.replace(/\/$/, '');

const posts = (await getCollection('news')).sort(
  (a, z) => z.data.date.valueOf() - a.data.date.valueOf()
);
---

<Layout title="News" description="Latest updates from Starflight Dynamics.">
  <Nav />

  <main>
    <!-- HEADER -->
    <header class="news-header">
      <h1><span class="white">LATEST</span><br /><span class="green">SFDY NEWS</span></h1>
    </header>

    <!-- ARTICLE LIST -->
    <div class="news-list">
      {posts.map(post => {
        const formatted = post.data.date.toLocaleDateString('en-GB', {
          day: 'numeric', month: 'long', year: 'numeric',
        });
        return (
          <a href={`${b}/news/${post.slug}`} class="article-card">
            <div class="article-thumb">
              {post.data.image
                ? <img src={`${b}${post.data.image}`} alt={post.data.title} loading="lazy" />
                : <div class="article-thumb-placeholder">✦</div>
              }
            </div>
            <div class="article-info">
              <time class="article-date" datetime={post.data.date.toISOString()}>{formatted}</time>
              <h2 class="article-title">{post.data.title}</h2>
              <p class="article-summary">{post.data.summary}</p>
              <span class="article-read">Read more →</span>
            </div>
          </a>
        );
      })}
    </div>
  </main>

  <Footer />
</Layout>

<style>
  main {
    min-height: 100vh;
    background: var(--bg);
  }

  /* HEADER */
  .news-header {
    padding: 8rem 2rem 4rem;
    text-align: center;
    border-bottom: 1px solid var(--border);
  }

  .news-header h1 {
    font-size: clamp(3rem, 9vw, 7rem);
    font-weight: 700;
    line-height: 1.05;
    letter-spacing: 0.02em;
  }

  .white { color: var(--text); }
  .green { color: var(--accent); }

  /* LIST */
  .news-list {
    max-width: 980px;
    margin: 0 auto;
    padding: 3rem 2rem 6rem;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .article-card {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 2rem;
    padding: 2rem 0;
    border-bottom: 1px solid var(--border);
    transition: background 0.15s;
  }
  .article-card:hover { background: rgba(255,255,255,0.02); }

  .article-thumb {
    width: 280px;
    aspect-ratio: 16/10;
    overflow: hidden;
    border-radius: var(--radius);
    background: var(--bg-card);
    flex-shrink: 0;
  }
  .article-thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s;
  }
  .article-card:hover .article-thumb img { transform: scale(1.04); }
  .article-thumb-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    color: var(--border);
  }

  .article-info {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    justify-content: center;
  }

  .article-date {
    font-family: var(--font-head);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--accent);
  }

  .article-title {
    font-size: clamp(1rem, 2vw, 1.35rem);
    font-weight: 700;
    color: var(--text);
    line-height: 1.3;
  }

  .article-summary {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.65;
  }

  .article-read {
    margin-top: 0.5rem;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--accent);
    font-family: var(--font-head);
    letter-spacing: 0.04em;
  }

  @media (max-width: 680px) {
    .article-card { grid-template-columns: 1fr; }
    .article-thumb { width: 100%; }
  }
</style>
